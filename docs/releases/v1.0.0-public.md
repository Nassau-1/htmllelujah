# HTMLlelujah 1.0.0

HTMLlelujah is an offline-first, AI-native presentation editor for Windows. It keeps
the editing model approachable for people used to slide software while storing decks
in an open, deterministic format that local tools and agents can inspect and change.

## What is included

- A native Windows editor with slide thumbnails, canvas editing, history, grouping,
  layers, locking, visibility controls, alignment guides, snapping, grids, themes,
  layouts, and reusable masters.
- Rich text, bullet lists, tables, images, shapes, connectors, icons, and round flag
  assets.
- Local `.hdeck` documents with atomic save, recovery, forward-version refusal, and
  portable embedded assets.
- Full-screen presentation mode plus standalone offline HTML and exact-page PDF
  export.
- Authenticated local-network collaboration with one authoritative host, ordered
  operations, reconnect handling, and visible expiring text locks.
- A packaged local MCP bridge for bounded reads, proposed mutations, explicit
  approval, commit, and undo workflows.

## Platform and installation

This release targets Windows 11 x64. The installer is deliberately named and
published as unsigned, so Windows may display a reputation warning. Verify the
SHA-256 checksum from the release assets before installing.

This is an official noncommercial public V1 governed by the PolyForm Noncommercial
1.0.0 terms in `LICENSE.txt`. The source is available, but this is not an Open Source
release: work for a commercial purpose, ordinary for-profit business use, and
anticipated commercial applications require a separate written commercial license.
The Electron/FFmpeg
compliance record is an engineering review, not legal advice; qualified legal
approval of the source-availability mechanism remains a prerequisite for this public
binary distribution.

HTMLlelujah does not require an internet connection for ordinary editing,
presentation, save/reopen, or export workflows. Collaboration is local-network only;
there is no hosted relay or cloud account.

## Known boundaries

- Consumer synchronized folders transport deck snapshots but cannot provide a
  cross-machine writer lock. Choose one host and join its LAN session when editing
  together.
- Hosted templates, cloud comments, offline merge, animations, transitions, speaker
  notes, formulas, linked charts, advanced freeform paths, and boolean vector
  operations are outside this release.
- Exported HTML is a presentation output, not an editable interchange format.
- The supported compact editor layout starts at 1080 CSS pixels of width.
- Future unknown `.hdeck` schema versions are refused and left unchanged.

## Verifiable release assets

The release includes the Windows installer and blockmap, checksums, separate
CycloneDX build/runtime and locked production-dependency SBOMs, content inventory,
candidate manifest, integrity evidence, the candidate-bound functional validation
manifest and evidence ZIP, canonical security evidence, and a deterministic final
release record. The security record binds zero-vulnerability production and full
pnpm audits, the exact successful CodeQL analysis and zero open alerts, a valid
Microsoft-signed Defender scanner with stable engine and signature identities, clean
no-remediation scans of the installer and unpacked application, and the exact unsigned
signature state of both executables. The build/runtime record also reads every known
Electron fuse from the exact packaged executable and rejects an unknown, missing,
inherited, or contrary state. The functional pair records the opened Windows UI,
exports, packaged MCP, accessibility scaling, text locks, installer lifecycle,
performance limits, and the
30-minute loopback collaboration soak together with its declared external
limitations. The final record binds those hashes, the clean source commit and tree,
lockfile, dependency graph, full artifact inventory, local annotated tag object,
remote annotated tag object, repository identity, and public release notes. The
publication workflow re-downloads every allowlisted asset and checks its size and
SHA-256 digest before it accepts the draft or public release.

The zero-vulnerability claim above applies to the two exact pnpm dependency audits.
The native-runtime SBOM is a version/hash inventory and Defender is an antimalware
scan; this V1 evidence does not claim independent native CVE coverage.
